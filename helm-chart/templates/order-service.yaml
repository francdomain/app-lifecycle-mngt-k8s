{{- if .Values.orderService.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: order-service
    tier: backend
spec:
  type: {{ .Values.orderService.service.type }}
  ports:
  - name: http
    port: {{ .Values.orderService.service.port }}
    targetPort: {{ .Values.orderService.service.targetPort }}
    protocol: TCP
  selector:
    app: order-service

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: order-service
    tier: backend
spec:
  replicas: {{ .Values.orderService.replicaCount }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
      - name: order-service
        image: {{ .Values.orderService.image.repository }}:{{ .Values.orderService.image.tag }}
        imagePullPolicy: {{ .Values.global.imagePullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.orderService.service.targetPort }}
          protocol: TCP

        startupProbe:
          httpGet:
            path: /status/200
            port: {{ .Values.orderService.service.targetPort }}
          initialDelaySeconds: {{ .Values.orderService.healthChecks.startup.initialDelaySeconds }}
          periodSeconds: {{ .Values.orderService.healthChecks.startup.periodSeconds }}
          timeoutSeconds: {{ .Values.orderService.healthChecks.startup.timeoutSeconds }}
          failureThreshold: {{ .Values.orderService.healthChecks.startup.failureThreshold }}

        readinessProbe:
          httpGet:
            path: /status/200
            port: {{ .Values.orderService.service.targetPort }}
          initialDelaySeconds: {{ .Values.orderService.healthChecks.readiness.initialDelaySeconds }}
          periodSeconds: {{ .Values.orderService.healthChecks.readiness.periodSeconds }}
          timeoutSeconds: {{ .Values.orderService.healthChecks.readiness.timeoutSeconds }}
          failureThreshold: {{ .Values.orderService.healthChecks.readiness.failureThreshold }}

        livenessProbe:
          httpGet:
            path: /status/200
            port: {{ .Values.orderService.service.targetPort }}
          initialDelaySeconds: {{ .Values.orderService.healthChecks.liveness.initialDelaySeconds }}
          periodSeconds: {{ .Values.orderService.healthChecks.liveness.periodSeconds }}
          timeoutSeconds: {{ .Values.orderService.healthChecks.liveness.timeoutSeconds }}
          failureThreshold: {{ .Values.orderService.healthChecks.liveness.failureThreshold }}

        env:
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: feature-flags
              key: LOG_LEVEL

        resources:
          requests:
            cpu: {{ .Values.orderService.resources.requests.cpu }}
            memory: {{ .Values.orderService.resources.requests.memory }}
          limits:
            cpu: {{ .Values.orderService.resources.limits.cpu }}
            memory: {{ .Values.orderService.resources.limits.memory }}

      terminationGracePeriodSeconds: 30

{{- if .Values.orderService.autoscaling.enabled }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: order-service-hpa
  namespace: {{ .Values.global.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: order-service
  minReplicas: {{ .Values.orderService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.orderService.autoscaling.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.orderService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
{{- end }}
